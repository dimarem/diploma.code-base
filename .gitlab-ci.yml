stages:
  - module-pipelines
  - infra-pipelines

frontend:
  stage: module-pipelines
  trigger:
    include:
      - "/frontend/.gitlab-ci.yml"
    strategy: depend
  only:
    changes:
      - frontend/**/*

backend:
  stage: module-pipelines
  trigger:
    include:
      - "/backend/.gitlab-ci.yml"
    strategy: depend
  only:
    changes:
      - backend/**/*

# тригерим пайплайн в инфраструктурном репозитории
trigger_frontend:
  stage: infra-pipelines
  trigger:
    project: std-046-13/momo-store-infra
    branch: master
    strategy: depend
  variables:
    FRONTEND_IMAGE: $CI_REGISTRY_IMAGE/frontend:$CI_COMMIT_SHA
    VERSION: 1.0.${CI_PIPELINE_ID}
  only:
    changes:
      - frontend/**/*

# тригерим пайплайн в инфраструктурном репозитории
trigger_backend:
  stage: infra-pipelines
  trigger:
    project: std-046-13/momo-store-infra
    branch: master
    strategy: depend
  variables:
    BACKEND_IMAGE: $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHA
    VERSION: 1.0.${CI_PIPELINE_ID}
  only:
    changes:
      - backend/**/*
  